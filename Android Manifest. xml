<?xml version="1.0" encoding="utf-8"?>
<!-- Replace your existing AndroidManifest.xml with this to ensure permissions -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required to download the Handwriting Recognition Model -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required for App Lock -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="SecureNotes"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SecureNotes"
        tools:targetApi="31">
        
        <!-- Note: We use FragmentActivity to support Biometrics -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SecureNotes">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
